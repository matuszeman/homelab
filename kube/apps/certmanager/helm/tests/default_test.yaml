suite: default
values:
  - values/required.yaml
templates:
  - charts/app/templates/*.yaml
excludeTemplates:
  - charts/app/templates/crd-*.yaml
tests:
  - it: renders
    asserts:
      - matchSnapshot: {}
