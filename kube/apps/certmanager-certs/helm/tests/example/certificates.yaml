issuers:
  configs:
    secrets:
      secret: {}

certificates:
  my-cert:
    enabled: true
    commonName: example.com
    dnsNames:
      - example.com
      - "*.example.com"
    issuer: letsencrypt
